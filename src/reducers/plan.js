const initialState = {
    address:null
}

const planReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}

export default planReducer
